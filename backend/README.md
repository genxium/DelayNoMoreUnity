Instead of running the backend in an IDE, e.g. Visual Studio, it's more convenient to run it by just 

```
backend> dotnet run
```

for testing.

For production packaging, use 
```
backend> dotnet build -c Release
```
