using shared; using TMPro; using UnityEngine; using UnityEngine.UI;  public class ArenaSettlementColumn : MonoBehaviour {     public Image avatar;     public TMP_Text beatsCnt;     public TMP_Text beatenCnt;      public void SetCharacter(CharacterDownsync chd) {         var newChConfig = Battle.characters[chd.SpeciesId];         AvatarUtil.SetAvatar2(avatar, newChConfig);          beatsCnt.text = chd.BeatsCnt.ToString();         beatenCnt.text = chd.BeatenCnt.ToString();     } } 